resources:
  jobs:
    silver_countries_parser:
      name: silver_countries_parser
      tasks:
        - task_key: parse_countries_task
          spark_python_task:
            python_file: ${workspace.file_path}/src/2_silver/reference/countries_job.py
            parameters:
              - --root_path
              - "${workspace.file_path}"
          environment_key: default
      queue:
        enabled: true
      environments:
        - environment_key: default
          spec:
            environment_version: "5"
      performance_target: PERFORMANCE_OPTIMIZED
      max_concurrent_runs: 1
