resources:
  jobs:
    monthly_references_bronze_ingestion:
      name: monthly_references_bronze_ingestion
      tasks:
        - task_key: bronze_monthly_task
          spark_python_task:
            # python_file: ../src/1_bronze/daily.py
            python_file: ${workspace.file_path}/src/1_bronze/monthly_references_ingeston.py
            parameters:
              - --root_path
              - "${workspace.file_path}"
          environment_key: default
      queue:
        enabled: true
      environments:
        - environment_key: default
          spec:
            environment_version: "5"
      performance_target: PERFORMANCE_OPTIMIZED
      max_concurrent_runs: 1
      schedule:
        quartz_cron_expression: 25 14 7 20 * ?
        timezone_id: Europe/Berlin
        pause_status: UNPAUSED


# resources:
#   jobs:
#     bronze_api_daily_job:
#       name: bronze-api-daily-job

#       schedule:
#         quartz_cron_expression: "0 0 2 * * ?"
#         timezone_id: Europe/Berlin
#         pause_status: UNPAUSED

#       environments:
#         - environment_key: default
#           spec:
#             environment_version: "2"
#             dependencies:
#               - requests==2.32.5

#       tasks:
#         - task_key: run_bronze_main
#           environment_key: default
#           spark_python_task:
#             python_file: ${workspace.file_path}/src/bronze/main.py