resources:
  pipelines:
    gold_presentation_pipeline:
      name: gold_presentation_pipeline
      catalog: lufthansa_level
      schema: gold
      serverless: true
      continuous: false
      libraries:
        - file:
            path: ${workspace.file_path}/src/3_gold/presentation/map_mater_views.py
      configuration:
        root_path: ${workspace.file_path}
        catalog: lufthansa_level
        schema: gold
