resources:
  pipelines:
    ingestion_pipeline_autoload:
      name: ingestion_pipeline_autoload
      catalog: lufthansa_level
      schema: bronze
      serverless: true
      continuous: false
      libraries:
        - file:
            path: ${workspace.file_path}/src/1_bronze/references_autoloader_ingest.py
            
      configuration:
        root_path: ${workspace.file_path}
        catalog: lufthansa_level
        schema: bronze
        landing_area: landing_area
        meta_volume: autoloader_metadata

