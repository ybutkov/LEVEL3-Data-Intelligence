resources:
  pipelines:
    operations_gold_pipeline:
      name: operations_gold_pipeline
      catalog: lufthansa_level
      schema: gold
      serverless: true
      continuous: false
      libraries:
        - file:
            path: ${workspace.file_path}/src/3_gold/operations_analytics_job_dlt.py
      configuration:
        root_path: ${workspace.file_path}
        catalog: lufthansa_level
        schema: gold
