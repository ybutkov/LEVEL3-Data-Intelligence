resources:
  pipelines:
    op_cdc_silver:
      name: op_cdc_silver
      catalog: lufthansa_level
      schema: silver
      serverless: true
      continuous: false
      libraries:
        - file:
            path: ${workspace.file_path}/src/2_silver/operations/operations_cdc_job.py
      configuration:
        root_path: ${workspace.file_path}
        catalog: lufthansa_level
        schema: silver

